git add *** (*** = -A for alt) (*** = filename for enkel fil)
git commit -m 'besked (havd er opdateret)'
git pull (henter andres ændringer ned)
git push (origin master) (hvis første gang i projekt benyte -u) (-u er upstream )

